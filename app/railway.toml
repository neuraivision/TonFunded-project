[build]
builder = "RAILPACK"
buildCommand = "npm install --legacy-peer-deps && npm run build"

[build.env]
RAILPACK_INSTALL_CMD = "npm install --legacy-peer-deps"

[deploy]
startCommand = "npx serve -s dist -l $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
